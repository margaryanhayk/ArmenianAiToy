<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ArmenianAiToy.Tools.ContentPackBuilder</RootNamespace>
  </PropertyGroup>

  <!--
    Application only — for InMemoryCuratedStoryLibrary (story id / title /
    bedtime-safe → manifest). The testable core (ContentPack.cs) has NO
    Application dependency; the test project links that file directly.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\backend\src\ArmenianAiToy.Application\ArmenianAiToy.Application.csproj" />
  </ItemGroup>

</Project>
